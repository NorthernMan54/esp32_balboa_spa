// Update as needed and copy to config.h

#define _WIFI_SSID "xxxxx"
#define _WIFI_PASSWORD "xxxx"
#define _BROKER "xxxxx"
#define _BROKER_LOGIN "xx"  // Can be "" if no login is required
#define _BROKER_PASS "xx"   // Can be "" if no password is required